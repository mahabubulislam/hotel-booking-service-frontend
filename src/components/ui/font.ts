import { Marcellus, Satisfy } from 'next/font/google'

export const satisfy = Satisfy({ weight: '400', subsets: ['latin'] })

export const marcellus = Marcellus({
  weight: '400',
  subsets: ['latin']
})
